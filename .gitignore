# Munzona oficial do TSE baixado sob demanda pelos scripts (grande e regeneravel;
# versoes commitadas chegaram corrompidas por conversao CRLF).
scratch/munzona_clean/

# Bytecode Python
__pycache__/
*.pyc

# Dados brutos de 1994 (muito grandes para o Git)
1994/**/*.csv
1994/*.zip

# Perfil do eleitorado 2026 baixado do CDN do TSE (~2,5 GB, regeneravel por
# scripts/baixar_perfil_eleitorado_2026.py). So os agregados por local de votacao
# em resultados_geo/eleitorado_2026/ entram no repo.
scratch/eleitorado/2026/

# Shapefiles de regiao do IBGE (280 MB; cada .shp tem 47-92 MB, acima do aviso de
# 50 MB do GitHub). Sao insumo de scripts/gerar_malhas_regioes.py — o que o site
# usa e a saida em resultados_geo/regioes_*/ (13,5 MB) e regioes_index.json.
malhas/

# Analise avulsa das regioes imediatas mais oscilantes nas presidenciais. Nao e
# usada pelo site — nem o .md nem o script sao carregados por eleicoes.html.
# Ficam no disco de quem gerou, fora do controle de versao.
analise_swing_regioes_imediatas.md
scripts/analisar_swing_rgi.py

